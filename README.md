To use the script, drag main.py into your maya script editor.
Please modify the installation path in the first lines of main.py so it actually finds the folder. Change all the "\" (backslashes) to "/"(regular slashes) for it to work properly.
